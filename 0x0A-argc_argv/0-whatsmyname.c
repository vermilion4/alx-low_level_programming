#include <stdio.h>
#include <stdlib.h>

/**
 * main - entry point
 *
 * @argv: string parameter
 * @argc: argument count (unused)
 * Return: 0
 */

int main(__attribute__((unused)) int argc, char **argv)
{
printf("%s", argv[0]);
return (0);
}
